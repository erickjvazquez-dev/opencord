# Go
/bin/
*.test
*.out

# Node / web
web/node_modules/
web/dist/
# Embedded SPA build output — only the committed placeholder index.html is tracked;
# the real index.html + assets/ are produced at Docker build time.
internal/webui/dist/assets/
# TypeScript build artifacts (emitted by `tsc -b`)
*.tsbuildinfo
web/vite.config.js
web/vite.config.d.ts

# Local env + per-project framework config (may contain secrets)
.env
.ccf/project.env

# Claude Code loop artifacts (transient lock written by /self-improve-opencord)
.claude/scheduled_tasks.lock

# OS
.DS_Store
